schema_ref: skill-pack.schema.yaml
kind: VerdifySkillPack
schema_version: "1.0"
name: crm-email
display_name: CRM Email
description: CRM-backed email drafting, review, send/log workflows, and safe crm.verdify.ai API access using credential references.
category: integration
maturity: experimental
includes:
  required:
    - crm-email
  optional: []
provides:
  capabilities:
    - crm-email
    - email-drafting
    - crm-context
    - activity-logging
depends_on:
  packs:
    - research-analysis
  tools:
    - crm-api
conflicts: []
install:
  hosts:
    - codex
    - claude
  default_profile: crm-operator
