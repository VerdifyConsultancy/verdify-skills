<!-- Fleet contract sync template: use this only for a centrally-rendered
     agent-fleet CI/CD contract refresh from jvallery/agents (#3577, #3044).
     Apply the `verdify:fleet-contract-sync` label. The label alone is not
     enough: pull-request-policy and critic-gate both additionally require
     a mechanical proof (Verdify::ManagedContractDiff) that this PR's diff
     touches only:
       - the sentinel-delimited span in AGENTS.md and/or CLAUDE.md (nothing
         outside "<!-- BEGIN agent-fleet CI/CD contract ... -->" /
         "<!-- END agent-fleet CI/CD contract ... -->" may change, and
         those sentinels must appear exactly once each, in order),
       - the generated .agent-fleet/ci.yaml (keeping its
         "# GENERATED by jvallery/agents" header),
       - MANIFEST.sha256 (regenerate it with `make manifest`).
     A PR that carries this label but changes anything else is rejected,
     not silently downgraded to a weaker check. Everything else -- an
     ordinary docs/chore change, a code change, a first-time adoption of
     the contract on a repo that doesn't have the sentinels yet -- uses
     the standard or lightweight template instead. -->

## Backlog issue

Closes #<!-- issue number -->

## Outcome

<!-- What the refreshed contract now says, and why (e.g. which
     jvallery/agents registry/render change this picks up). -->

## Evidence

<!-- `make manifest` was run; `make test` passed; the diff below the
     sentinel span in AGENTS.md/CLAUDE.md is empty. -->
