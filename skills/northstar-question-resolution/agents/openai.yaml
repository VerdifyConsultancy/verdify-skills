interface:
  display_name: "North Star Question Resolution"
  short_description: "Research and resolve gated questions"
  default_prompt: "Use $northstar-question-resolution to inventory human-gated questions, research options, ingest evidence, and record delegated default answers."
policy:
  allow_implicit_invocation: true
