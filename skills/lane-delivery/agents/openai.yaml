interface:
  display_name: "Verdify Lane Delivery"
  short_description: "Implement and close one leased lane"
  default_prompt: "Verify the separate approved dispatch commit D, keep its plan/wave/contract artifacts immutable, implement only the assigned lane in its leased worktree, validate and commit implementation head I, then commit only the worker closeout as evidence head E. Push the linked PR and stop writing so a different critic agent/session can review E."
policy:
  allow_implicit_invocation: false
