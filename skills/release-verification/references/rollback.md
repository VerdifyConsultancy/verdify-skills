# Rollback

Before deployment identify rollback trigger, authorized operator, prior known-good revision, data compatibility, migration reversal or forward-fix strategy, and verification after rollback.

A rollback is itself a deployment requiring evidence. Preserve incident context and create follow-up issues rather than treating rollback as closure.
