# Traceability

Minimum chain:

```text
source or approved decision
  -> requirement
  -> product workflow or design surface
  -> architecture component/module
  -> sprint acceptance criterion
  -> lane criterion
  -> evidence
```

A missing link is either a gap or deliberate non-traceable context. Record exceptions explicitly. Never renumber stable IDs merely for presentation.
