interface:
  display_name: "Verdify Project Definition"
  short_description: "Define discovery, requirements, product, and surfaces"
  default_prompt: "Continue the earliest incomplete project-definition mode. Use supplied evidence, write the canonical artifact and human view, and open a gate for material ambiguity."
policy:
  allow_implicit_invocation: true
