# Gate management

A gate records type, question, owner, evidence required, allowed decisions, decision, rationale, timestamp, and resume state. The orchestrator may resolve only gates delegated by policy.

Human control is required for unresolved product intent, public-interface changes, migrations, security boundaries, destructive operations, production deployment, and policy exceptions unless a repository policy explicitly delegates that class.
