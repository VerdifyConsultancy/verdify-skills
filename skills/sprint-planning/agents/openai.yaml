interface:
  display_name: "Verdify Sprint Planning"
  short_description: "Plan issues, lanes, and contracts atomically"
  default_prompt: "Plan a bounded sprint from ready GitHub Issues. Create the lane topology and contracts in one transaction, then stop at plan approval without creating worktrees."
policy:
  allow_implicit_invocation: true
