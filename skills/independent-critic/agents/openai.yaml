interface:
  display_name: "Verdify Independent Critic"
  short_description: "Fresh review of lane scope, code, and evidence"
  default_prompt: "At closeout-only head E, verify approved dispatch D and independently review implementation I using a different agent, session, and detached worktree. Change only the canonical critic report, commit it as S, and hand S to a repository admin or maintainer other than the PR author for approval."
policy:
  allow_implicit_invocation: false
