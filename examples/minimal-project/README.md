# Minimal validated project

This fixture demonstrates the complete durable artifact chain for one GitHub issue, one module, one lane, one worker worktree/session, one pull request, one critic review, and one verified deployment.

The values are illustrative. Run `ruby scripts/validate-repo.rb` from the package root to validate every YAML artifact against its `schema_ref`.
