# Autonomous Platform Shutdown Handoff

Prepared 2026-07-11T01:05:48Z from protected `dev@4fe6ff65338bb129cbfbeb2451cb5a5fa696533d`.

Canonical record: `sprint-handoff.yaml`

Remote retrieval: draft PR #227 or `origin/handoff/autonomous-platform-reset-2026-07-11`.

## Human Eyes On

None required to restart. The next ROOT agent has delegated authority to re-probe, finish or supersede draft planning work, manage PRs, and continue issue-backed execution. The three Agent Platform P0 issues remain machine-enforced program stops, not requests to begin the four-project slice.

## What Is Banked

Issues #135, #215, and #71 are closed and terminal. Issue #71 delivered fail-closed route-authority loading, legal producer transitions, ignored derived route caches, fresh S2 criticism, protected-dev evidence merge `8487e662`, and normal receipt merge `4fe6ff65`.

Current proof:

- `origin/dev` is `4fe6ff65338bb129cbfbeb2451cb5a5fa696533d`.
- `terminal_at?(issue-71)` is true.
- `integrated_unterminated_sprints` is empty.
- Global routing returns `STATE_OF_UNION_STALE -> state-of-union/strategy-refresh`.
- The root checkout is clean and every current issue #135/#71 worker and critic lease is released.

PRs #221 and #222 are explicit premature-merge incidents. PR #223 integrated exact fresh-critic evidence under a recorded non-author-review exception. None of those events should become routine precedent; issue #70 owns typed merge authority and proportional policy.

## Checkpointed Work

Draft PR #226 is the post-#71 strategy checkpoint:

- Branch: `origin/planning/post-71-state-of-union`
- Head: `46318545fd04930e8d3e711de2e1e1c974f9ab5f`
- Diff: exactly the three canonical strategy artifacts
- Local validation: both YAML schemas, repository validation, and diff check pass
- Intended route: issue #73 as the sole next candidate
- Known CI state at capture: compliance and validate passed; policy/delivery/critic failed on the known planning-only metadata mismatch

Issue #211 is open only to own that refresh. The next agent must re-probe PR #226 and either finish it or supersede it explicitly. Do not begin issue #73 implementation on the strategy branch.

The branch `origin/quarantine/issue71-root-artifact-misapply` is preserved only as forensic context. Never merge it.

## Next Candidate

Issue #73 fixes demonstrated vacuous critic approval:

- approving assessments must be non-empty and cover every lane criterion exactly once;
- every approving assessment must be satisfied with evidence;
- partial or empty non-approving reports remain allowed, but supplied IDs must be unique and contract-valid;
- closeout IDs are a truthful contract-valid subset because E cannot claim future S, CI, integration, deployment, or outcome evidence;
- already-correct empty verified-release rejection is pinned, not rebuilt;
- empty request-fixes assessment remains valid;
- vacuous evidence must not authorize review inbox, merge, or terminalization.

Issue #225 owns structured lifecycle-phase schema and impossible-cycle prevention. It is a discovered follow-up, not permission to expand #73 silently.

## Program Sequence

1. Finish or supersede PR #226, merge the bounded strategy refresh, and verify project-router selects #73.
2. Execute #73, then #75 and #74.
3. Execute #43 and #70 to codify the bounded loop, risk classes, typed stops, auto-advance, and removal of avoidable human gates.
4. Resolve Agents #2884, #2887, #2906, #2890, and #2905.
5. Establish provider negotiation through Verdify #12/#116 and Agents #2497/#655.
6. Complete Gravity #184/#407.
7. Complete Orbit #193-#198 and #43.
8. Run one four-project transaction with one correlation ID.
9. Run failure drills and seven unattended days before broader autonomy.

Do not start step 8 while Agents #2884, #2887, or #2906 remains open.

## Cold Start

Use `restart-prompt.md` verbatim with the next Codex instance. Its first action is a fresh project-router and GitHub snapshot, not implementation.
