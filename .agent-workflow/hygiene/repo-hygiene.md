# Repository hygiene

Wave 0 is complete at
`dev@442069ebc49f8622f2ac1f72a4d4ccbee3206564` with four approved,
issue-backed exception groups. Completion authorizes bounded sprint planning;
it does not authorize npm publication, main promotion, live policy mutation, or
destructive ref cleanup.

Passing controls include repository identity, local instructions, typed
authority, approved North Star/project/architecture/strategy artifacts, GitHub
backlog ownership, module boundaries, a clean primary checkout, current dev CI,
and a typed repo-controller scope. A clean detached full gate verified manifest
`403/403`, `90` runs and `531` assertions, CLI, PR policy, release preflight,
and npm install at the assessed head.

Approved warnings remain durable in GitHub:

- #130 owns ignored and untracked host-file exclusion from manifests and packages.
- #126 owns selective recovery, the final ref ledger, and branch/worktree cleanup.
- #120 owns exact artifact publication, history remediation, and resumable release state.
- #121 owns live dev/main protections and current-head approval transport.

The ignored `.claude/scheduled_tasks.lock` file was not deleted. Recovery refs
`fe1f94e` and `f425515` were not merged or removed. The next handoff is a
state-of-union baseline refresh after this hygiene transaction, followed by
sprint planning for separate #130 and #126 lanes with #126 dependent on #130.
